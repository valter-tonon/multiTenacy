// @flow
import * as React from 'react';
import Menu from "../Menu";


const Marcas = (props) => {
    return (
        <Menu title={'Marcas'} user={props.user}>

        </Menu>
    );
};

export default Marcas
